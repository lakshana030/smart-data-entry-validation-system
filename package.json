{
  "name": "smart-data-entry-validation-system",
  "version": "1.0.0",
  "description": "Smart Data Entry Validation System Root Package",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "dev": "npm start --prefix backend"
  },
  "dependencies": {
    "helmet": "^8.1.0"
  }
}
